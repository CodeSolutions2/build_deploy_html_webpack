{
  "homepage": "https://codesolutions2.github.io/temp_repo_GithubPagesDeploymentTests/",
  "name": "index",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "build": "npm run build",
  "deploy": "gh-pages -d build",
  "scripts": {
    "build": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
